# Copy this file to .env (which is gitignored) and paste your real token.
# Get it from: Todoist -> Settings -> Integrations -> Developer -> "API token"
# NEVER commit the real .env file. Never `git add -f .env`.
TODOIST_API_TOKEN=
